# Template Block: Business Rules

Use this block to document business rules gathered during the smart-interview phase.
Each rule must express a condition → constraint/action pair that translates directly into conditional logic.

---

## Business Rules

- **BR1**: If [condition], then [constraint or enforced action]. Exception: [edge case or override, if any]
- **BR2**: If [condition], then [constraint or enforced action]. Exception: [edge case or override, if any]
- **BR3**: If [condition], then [constraint or enforced action].

<!-- Add or remove rows as needed. Rules without exceptions can omit the Exception field. -->
